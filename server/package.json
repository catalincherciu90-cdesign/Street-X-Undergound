{
  "name": "gps-tracker-server",
  "version": "1.0.0",
  "description": "Platformă GPS Tracker — Cloudflare Worker + D1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute gps-tracker --file=./schema.sql --remote",
    "db:init:local": "wrangler d1 execute gps-tracker --file=./schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
